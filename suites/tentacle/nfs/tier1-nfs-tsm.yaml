#===============================================================================================
#---    Test Suite for NFS-Ganesha TSM (Transparent State Migration)   ---
#===============================================================================================
# Conf: conf/tentacle/nfs/1admin-7node-3client.yaml
#
# Purpose:
#   Automate NFS-Ganesha Transparent State Migration (TSM) validation.
#   TSM syncs NFSv4 client state (opens/locks/delegations) across Ganesha peers
#   via enable_TSM / Tsm_Port so HA/failover can reclaim state.
#
# Requirements:
#   - ≥2 NFS-capable nodes (node2/node3 in the default conf)
#   - Product build that renders enable_TSM / Tsm_Port in ganesha.conf
#
# Layout:
#   1. Bootstrap + clients
#   2. Basic TSM enablement (conf + listen + IO)
#   3. Peer sync (Ceph_Nodes, ESTAB, recovery log markers)
#   4. Negative — single-daemon TSM must not look multi-peer healthy
#
#===============================================================================================
tests:
  # - test:
  #     abort-on-fail: true
  #     desc: Install software pre-requisites for cluster deployment.
  #     module: install_prereq.py
  #     name: setup pre-requisites

  # - test:
  #     abort-on-fail: true
  #     config:
  #       steps:
  #         - config:
  #             command: bootstrap
  #             service: cephadm
  #             args:
  #               mon-ip: node1
  #         - config:
  #             command: add_hosts
  #             service: host
  #             args:
  #               attach_ip_address: true
  #               labels: apply-all-labels
  #         - config:
  #             command: apply
  #             service: osd
  #             args:
  #               all-available-devices: true
  #         - config:
  #             command: apply
  #             service: rgw
  #             pos_args:
  #               - rgw.1
  #             args:
  #               placement:
  #                 label: rgw
  #         - config:
  #             args:
  #               - "ceph fs volume create cephfs"
  #             command: shell
  #         - config:
  #             args:
  #               placement:
  #                 label: mds
  #             base_cmd_args:
  #               verbose: true
  #             command: apply
  #             pos_args:
  #               - cephfs
  #             service: mds
  #         - config:
  #             args:
  #               - "ceph osd pool create rbd"
  #             command: shell
  #         - config:
  #             args:
  #               - "rbd pool init rbd"
  #             command: shell
  #     desc: bootstrap and deploy services.
  #     destroy-cluster: false
  #     polarion-id: CEPH-83573713
  #     module: test_cephadm.py
  #     name: Deploy cluster using cephadm

  # - test:
  #     abort-on-fail: true
  #     config:
  #       command: add
  #       id: client.1
  #       node: node4
  #       install_packages:
  #         - ceph-common
  #         - ceph-fuse
  #       copy_admin_keyring: true
  #     desc: Configure client.1
  #     destroy-cluster: false
  #     module: test_client.py
  #     name: configure client

  # - test:
  #     abort-on-fail: true
  #     config:
  #       command: add
  #       id: client.2
  #       node: node5
  #       install_packages:
  #         - ceph-common
  #         - ceph-fuse
  #       copy_admin_keyring: true
  #     desc: Configure client.2
  #     destroy-cluster: false
  #     module: test_client.py
  #     name: configure client

  # - test:
  #     abort-on-fail: true
  #     config:
  #       command: add
  #       id: client.3
  #       node: node6
  #       install_packages:
  #         - ceph-common
  #         - ceph-fuse
  #       copy_admin_keyring: true
  #     desc: Configure client.3
  #     destroy-cluster: false
  #     module: test_client.py
  #     name: configure client

  # - test:
  #     abort-on-fail: true
  #     config:
  #       command: add
  #       id: client.4
  #       node: node7
  #       install_packages:
  #         - ceph-common
  #         - ceph-fuse
  #       copy_admin_keyring: true
  #     desc: Configure client.4
  #     destroy-cluster: false
  #     module: test_client.py
  #     name: configure client

  #---------------------------------------------------------------------------------------------
  # TSM feature tests
  #---------------------------------------------------------------------------------------------

  - test:
      name: NFS TSM - Basic enablement
      module: tsm.test_nfs_tsm_basic.py
      desc: >
        Deploy multi-node NFS with TSM, verify enable_TSM/Tsm_Port in ganesha.conf,
        TSM port listen, peer ESTAB, and basic client IO
      abort-on-fail: false

  # - test:
  #     name: NFS TSM - Peer sync and recovery markers
  #     module: tsm.test_nfs_tsm_peer_sync.py
  #     desc: >
  #       Validate Ceph_Nodes peer list, ESTAB TSM connections across daemons,
  #       and COMPONENT_TSM / tsm_recover_from_peers log markers
  #     abort-on-fail: false

  # - test:
  #     name: NFS TSM - Negative single daemon
  #     module: tsm.test_nfs_tsm_negative.py
  #     desc: >
  #       enable_tsm with placement count=1 must not present a healthy multi-peer
  #       TSM posture (deploy failure, missing conf, no ESTAB, or register errors)
  #     abort-on-fail: false
